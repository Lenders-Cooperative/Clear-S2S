============
Installation
============

At the command line::

    $ easy_install Django-clear-s2s

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv Django-clear-s2s
    $ pip install Django-clear-s2s
